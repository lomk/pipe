export class RemoteIp{
    id: number;
    address: string;
    mask: number;

    // constructor(address: string, mask: number){
    //     this.address = address;
    //     this.mask = mask;
    // }
}
